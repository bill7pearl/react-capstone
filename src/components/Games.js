/* import React from 'react';

const Games = ({
  image, title, description, publisher, releaseDate,
}) => {

  return (
    <main className="games-container">
      <div className="game-img"><img className="img-api" src={image} alt="" /></div>
      <div className="game-details">
        <h2>{title}</h2>
        <h4>{description}</h4>
      </div>
    </main>
  );
};

export default Games;
 */